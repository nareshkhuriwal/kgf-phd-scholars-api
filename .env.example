APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost
# API routes: raise PHP max_execution_time (0 = php.ini only). Helps Azure/PDF-heavy requests.
APP_API_MAX_EXECUTION_SECONDS=120

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=laravel
DB_USERNAME=root
DB_PASSWORD=

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=public
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

# Paper/file storage — container "scholars": library/… originals, reviews/… review working copies
AZURE_STORAGE_DISK=azure
PAPERS_UPLOAD_DISK=azure
# LIBRARY_BLOB_PREFIX=library
# REVIEW_WORKING_COPY_PREFIX=reviews
# true = read paper files only from PAPERS_UPLOAD_DISK (Azure). Set false only for local legacy debugging.
PAPERS_STRICT_UPLOAD_DISK_ONLY=true
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_CONTAINER=scholars
# If existing paper files were stored in another container (e.g. "papers"), set this so reads/clones still work.
AZURE_STORAGE_LEGACY_CONTAINER=papers
# Guzzle timeouts for Azure blob driver (seconds)
AZURE_HTTP_TIMEOUT=120
AZURE_HTTP_CONNECT_TIMEOUT=15

# Laravel Http:: defaults (library imports, etc.)
HTTP_CLIENT_TIMEOUT=25
HTTP_CLIENT_CONNECT_TIMEOUT=8

# CORS (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:5173,https://phd.khuriwalgroup.com,https://scholars.khuriwalgroup.com
